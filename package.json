{
  "name": "front-end-next-sandbox",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "npx @biomejs/biome format \"**/*.{ts,tsx,md}\" --write"
  },
  "devDependencies": {
    "@biomejs/biome": "1.6.3",
    "@repo/typescript-config": "workspace:*",
    "@repo/tailwind-config": "workspace:*",
    "turbo": "latest"
  },
  "packageManager": "pnpm@8.9.0",
  "engines": {
    "node": ">=18"
  }
}
